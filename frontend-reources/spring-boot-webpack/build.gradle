dependencies {
  compile "org.springframework.boot:spring-boot-starter-mustache"
}

clean.doFirst {
  [
      ".gradle",
      "src/main/resources/static",
  ].each { delete "${projectDir.getAbsolutePath()}/$it" }
}

clean.dependsOn ":spring-boot-webpack:ui:clean"
clean.shouldRunAfter ":spring-boot-webpack:ui:clean"

assemble.dependsOn ":spring-boot-webpack:ui:build"
assemble.shouldRunAfter clean, ":spring-boot-webpack:ui:build"
